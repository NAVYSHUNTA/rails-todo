<h1>詳細</h1>

<ul class="todo">
  <li><%= check_box_tag "todo_done_#{@todo.id}", "1", @todo.done, disabled: true %></li>
  <li><%= "タスク：#{@todo.task}" %></li>
  <li><%= "優先度：#{priority_label @todo.priority}" %></li>
  <li><%= "締切：#{@todo.deadline ? l(@todo.deadline) : "なし"}" %></li>
</ul>

<%= link_to "一覧へ戻る", todos_path, class: "button" %>
